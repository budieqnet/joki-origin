# Core
httpx>=0.27,<1.0
prompt_toolkit>=3.0,<4.0
curl_cffi>=0.15,<1.0
rich>=15.0,<16.0
pyte>=0.8.0,<1.0

# Dev
pytest>=7.0

# Optional (install jika butuh fitur tertentu)
# pyserial>=3.5              # untuk handle_serial_send
# openai-whisper>=20231117   # untuk audio_transcribe

# Tools
duckduckgo_search>=6.0       # untuk web_search (DDGS)
html2text>=2024.0            # untuk web_fetch markdown
